export type User = {
  id: string;
  username: string;
}

export type Wishlist = {
  id: string;
  title: string;
  description: string;
  cover: string;
}

export type Present = {
  id: string;
  title: string;
  description: string;
  cover: string;
  link: string;
}